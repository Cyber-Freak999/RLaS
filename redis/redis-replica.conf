port 6379
dir /data

# Must run the same AOF settings as the primary: the promoted replica inherits
# the durability guarantee, so a silent divergence here would evaporate it on
# the first failover.
appendonly yes
appendfsync everysec

replicaof 172.28.0.10 6379
